<UserControl x:Class="MyUserControl.Views.UserControl2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyUserControl.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="White">
        <Button Height="50" VerticalAlignment="Top" Content="subscribe" Command="{Binding SubscribeCommand}"/>
        <Button Height="50" VerticalAlignment="Top" Content="publish" Command="{Binding PublishCommand}"/>
        <TextBlock Text="{Binding MyTxt}"/>
    </StackPanel>
</UserControl>
